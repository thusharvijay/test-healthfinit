import React from 'react';
import ReactDOM from 'react-dom/client';
import PathologyApp from './app/root';
import './global.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <PathologyApp />
  </React.StrictMode>
);
